 static String catAndMouse(int x, int y, int z) {
        int catA=Math.abs(x-z);
        int catB=Math.abs(y-z);
        if(catA>catB)
            return "Cat B";
        else if(catB>catA)
            return "Cat A";
        else
            return "Mouse C";
    }
